# Agent versions the live labs last confirmed against lib/state.sh and
# lib/hooks/state-hook.sh. `am doctor` warns when an installed agent is newer
# than its pin: re-run the named lab, check report.txt + timeline.tsv for
# glyph/hook/state agreement, then update the line (the lab prints the
# installed version at the end of its run).
#
# Whitespace-separated: <agent binary> <version> <date> <lab> <note...>
claude 2.1.263 2026-09-07 tests/live_lab/run.sh s1-s7 agree with the 2.1.237 run; ✳ title static; ctrl-b Stop fires; s3 shows no dialog under a permissive policy
cursor-agent 2026.09.02 2026-09-07 tests/live_lab/run_cursor.sh c1-c7 agree; Ready/Working/Waiting title suffixes; tasks footer after "Add a follow-up"; resume fires no hook until the next prompt (title layer covers it)
pi 0.84.2 2026-09-07 tests/live_lab/run_pi.sh p1-p4 agree (session_start/agent_start/agent_settled, 200s quiet turn stays running, death -> idle)
