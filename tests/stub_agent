#!/usr/bin/env bash
# stub_agent - Fake agent for integration tests
# Stays alive until killed, prints a marker so tests can verify it started
echo "stub-agent-ready"
while IFS= read -r line; do
    echo "stub-agent-input:$line"
done
