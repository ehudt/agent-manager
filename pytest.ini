[pytest]
markers =
    integration: End-to-end tests that invoke the real sandbox lifecycle.
    security: Security-focused runtime assertions for sandbox containers.
    docker: Tests that require Docker daemon access.
    functional: Functional lifecycle assertions for sandbox containers.
    ux: User-facing output and error-message assertions for sandbox operations.
